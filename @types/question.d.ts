//d.ts declartion files not to run but to give context for code-hints for other files
export interface Question {
          title: string;
          content: string;
          answerCount?: number;
}
