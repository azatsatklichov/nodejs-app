/*
  Write a script to fix the corrupted files in ./files

  Each file has its data duplicated.
  Your should reduce each file content to exactly half its length
*/

const fs = require('fs');
const path = require('path');
