console.log(module);

//How require works,  require(‘module’)
// Resolving  - finds absolute paths
// Loading   
// Wrapping
// Evaluating 
// Caching   

let loda = require('lodash');

//if not LOAD(exec) but just to RESOLVE
//to check if installed or not only
let az = require.resolve('az-lada');
 