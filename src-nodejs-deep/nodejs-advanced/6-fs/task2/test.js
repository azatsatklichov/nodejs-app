/*
  You have a directory of many files at ./files

  Write a script that will delete all files
  older than 7 days from that directory.

  You can use the seed.js script
  to generate test data for the task
*/

const fs = require('fs');
const path = require('path');
